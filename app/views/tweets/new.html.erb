<%= bootstrap_form_with(model: @tweet, local: true) do |form| %>
  <%= render 'shared/error_messages', model: @tweet %>

  <div class="field">
    <%= form.text_area :text %>
  </div>

  <div class="actions">
    <%= form.submit t('.tweet') %>
  </div>
<% end %>
